query {
  votes (
    where: {
      created_gte: 1672000000
      created_lte: 1674000000
      space_in: ["stgdao.eth", "ens.eth", "aave.eth", "uniswap"]
      vp_state: "final"
    }
    orderBy: "created",
    orderDirection: desc
  ) {
    voter
  }
}
